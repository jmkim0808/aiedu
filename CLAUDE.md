# 작업 규칙

이 저장소에서 코드를 쓸 때 지키는 것들. 새로 합류한 사람도 이 파일만 읽고 시작할 수 있어야 한다.

## 전제

- 무엇을 만들지는 `docs/design/` 의 설계문서가 정한다. 문서가 비어 있으면 도메인을
  **추측해서 만들지 않는다.** 문서를 먼저 가져오거나 확인을 받는다.
- 사내 전산팀이 없다. 서버를 세우거나 상시 관리가 필요한 구성요소를 추가하지 않는다.

## 기술 제약

- 의존성은 `wrangler` 하나로 유지한다. 패키지를 추가하려면 그 이유를 커밋 메시지에 남긴다.
- **빌드 단계를 만들지 않는다.** 프론트엔드는 순수 HTML/CSS/JS 로 쓴다.
  번들러·트랜스파일러·프레임워크를 넣지 않는다.
- 데이터는 D1, 파일은 R2. 그 밖의 외부 저장소를 붙이지 않는다.

## 개인정보

- **이메일과 성명을 D1 에 저장하지 않는다.** 개인 식별은 사번으로만 한다.
- 이메일 ↔ 역할 매핑은 `ROLE_MAP` 시크릿에만 둔다. Worker 는 Access 가 넘겨준 이메일을
  역할코드로 바꾼 직후 버리고, 응답·로그·DB 어디에도 남기지 않는다.
- 이 규칙을 깨야 하는 요구가 나오면 구현하기 전에 먼저 알린다.

## 3법인 (HQ 본사 / SY 심양 / VP 빈푹)

- 법인별로 테이블을 나누지 않는다. 공통 테이블에 `entity_code` 컬럼을 둔다.
- 법인마다 다른 규칙(적용 시점, 필수 여부, 언어)은 코드 분기가 아니라 데이터로 표현한다.
- 해외법인 데이터를 다루는 기능을 추가할 때는 현지 노동법·개인정보보호법(중국 PIPL,
  베트남 개인정보보호령) 검토가 선행되어야 한다는 점을 구현 전에 짚는다.

## 운영 기준값

임계치·정책·계수처럼 운영 중에 바뀌는 값은 코드에 숨기지 않는다. 테이블 컬럼으로 노출해
담당자가 화면에서 바꿀 수 있게 한다. 반대로 산정 로직의 근간은 화면에서 못 바꾸게 막는다.

## 데이터베이스 변경

1. `db/migrations/NNNN_<무엇을>.sql` 을 새로 만든다.
2. 로컬에 먼저 적용하고 화면까지 확인한다. 운영에 먼저 올리지 않는다.
3. 확인이 끝나면 `db/schema.sql` 에 반영해 전체 스키마를 최신으로 유지한다.

## 보안

- `REQUIRE_ACCESS` 를 `"false"` 로 커밋하지 않는다. 로컬은 `.dev.vars` 로만 끈다.
- 시크릿·토큰·이메일 매핑을 저장소에 넣지 않는다. `wrangler secret` 으로만 관리한다.
- 인증 검사를 우회하는 경로를 만들지 않는다. `/api/health` 가 유일한 예외다.

## 주석과 커밋

- 주석은 "무엇을 하는지"가 아니라 "왜 이렇게 했는지"를 적는다. 특히 제약 때문에 택한
  선택(빌드 없음, 이메일 미저장 등)은 근거를 남긴다.
- 커밋 메시지는 한국어로, 무엇을 왜 바꿨는지 적는다. 검증한 내용도 함께 적는다.
